# 测试 ↔ 设计文档章节对照表（P1-4-1 的记账产物）

> **用途**：回答"设计文档要求的每一条，具体由哪条测试保证"。P1-4-1 的原文要求是
> 「覆盖 timeout-only、日志边界、工具结果非法、报告失败、owner 隔离、失败持久化、
> 越权 KB、脱敏、证据伪造和大输入」以及「**每条测试注明对应设计文档章节**」。
>
> **设计文档**：`docs/agent-mvp设计.md`（章节号即该文档的 `§`）。
>
> **维护规则**：新增测试文件时，在模块 docstring 顶部写一行
> `设计文档章节：§x.y、§z`（现有 30 个测试文件都已写上），并在本表补一行；
> 新增针对具体案例的测试，把案例号写进函数名与 docstring（例如
> `test_case_05_...`）。**表格与 docstring 不一致时以 docstring 为准。**
>
> 统计口径：`py -m pytest tests --collect-only -q` → **372 条**（2026-09-15，P1-5-1 完成时）。

---

## 1. §17.2「最少 10 条案例」逐条对照

| 案例 | 设计文档要求 | 覆盖测试 | 状态 |
| --- | --- | --- | --- |
| 1 | `502 + MySQL connection timeout` 正常分析 | `test_graph_workflow.test_graph_normal_path_runs_tool_observe_and_report`；`test_incident_service.test_execute_incident_persists_graph_result` | 已有 |
| 2 | `timeout` 但没有数据库关键词 | **新增** `test_design_cases_graph.test_case_02_timeout_only_log_completes_with_fault_log_evidence`（断言只命中 `timeout`、报告仍通过校验）；工具层 `test_logging_and_steps.test_timeout_only_log_is_detected_without_database_keywords` | 本次补齐 |
| 3 | 未知服务名 | **新增** `test_design_cases_graph.test_case_03_unknown_service_degrades_with_a_concrete_suggestion`（降级 + 建议 + 轨迹）；工具层 `test_tools_adapters.test_service_status_unknown_service_is_normalized` | 本次补齐 |
| 4 | 空日志或缺少标题 | **新增** `test_input_boundaries.test_case_04_blank_input_is_rejected`（6 种空/纯空白组合）、`test_case_04_oversized_input_is_rejected`；工具层 `test_logging_and_steps.test_analyze_log_still_rejects_empty_input` | 本次补齐 |
| 5 | 知识库无相关内容 | **新增** `test_design_cases_graph.test_case_05_empty_retrieval_cannot_be_reported_as_knowledge_base_evidence`（`ok=True` + `sources=[]` → 引用被剔除 → `insufficient_evidence`）；工具层 `test_tools_adapters.test_search_knowledge_no_results_is_successful` | 本次补齐 |
| 6 | 检索工具抛异常 | **新增** `test_design_cases_graph.test_case_06_search_tool_exception_degrades_with_a_normalized_code`；工具层 `test_tools_adapters.test_search_knowledge_timeout_is_normalized` | 本次补齐 |
| 7 | 服务状态工具超时 | **新增** `test_design_cases_graph.test_case_07_retrieval_timeout_is_reported_as_a_timeout` | **以检索超时替代**，原因见 §5 边界 1 |
| 8 | 报告模型返回非法 JSON | `test_graph_workflow.test_graph_marks_invalid_report_without_calling_tools_again`；`test_report_repair`（21 条：容错解析 + 一次受限修复） | 已有 |
| 9 | 模型重复请求相同工具 | **新增** `test_design_cases_graph.test_case_09_repeated_identical_tool_calls_are_recorded_and_bounded` | 本次补齐 |
| 10 | 达到最大循环次数 | `test_graph_workflow.test_graph_stops_at_max_model_iterations`；`test_design_cases_graph.test_case_09_...` 同时覆盖"上限截断" | 已有 |

## 2. P1-4-1 点名的十类场景

| 场景 | 覆盖位置 | 关键断言 |
| --- | --- | --- |
| timeout-only | `test_design_cases_graph.test_case_02_*`、`test_logging_and_steps.test_timeout_only_log_is_detected_without_database_keywords`、`test_model_timeout`（18 条模型/预算超时） | 只有 `timeout` 信号也能形成证据；模型超时不重试、错误码归一化 |
| 日志边界 | `test_input_boundaries`（工具参数上限 `20_000` 包含边界、缺参、超限）、`test_logging_and_steps`（空输入、信号每类封顶、行号与片段） | 上限 +1 必须失败，恰好等于上限必须成功 |
| 工具结果非法 | **新增** `test_tool_result_contract`（16 条）、`test_design_cases_graph.test_model_tool_call_with_a_missing_argument_is_normalized_not_crashed` | 7 种非法形状 → `INVALID_TOOL_RESULT` + 失败步骤 + 不泄露原始载荷 |
| 报告失败 | `test_report_repair`、`test_graph_workflow.test_graph_marks_invalid_report_*`、`test_report_evidence`、`test_degraded_summary` | 非法 JSON / 结构不合法 → `report_validation_failed` 并落库 |
| owner 隔离 | `test_run_access_and_failures`（14 条）、`test_run_history_summary.test_list_only_returns_runs_of_the_authenticated_owner` | 别人的 ID 与不存在的 ID 响应不可区分 |
| 失败持久化 | `test_run_access_and_failures`、`test_degraded_summary_persistence`（4 条） | `degraded` / `report_validation_failed` 真的落库并可回查 |
| 越权 KB | `test_incident_authorization_api`（4 条）、`test_knowledge_base_authorizer`（11 条） | 未授权/不存在 KB 不产生任何运行记录 |
| 脱敏 | `test_redaction`（15 条） | 原始凭据在落库行、`/runs` 响应、模型上下文里都找不到 |
| 证据伪造 | `test_evidence_verification`（23 条）、`test_report_evidence`、`test_untrusted_content` | 无法追溯的引用被剔除、置信度只降不升 |
| 大输入 | **新增** `test_input_boundaries`（超长拒绝 + 上限接受）、`test_design_cases_graph.test_large_log_run_stays_bounded_and_never_echoes_the_input` | 20_000 字日志端到端：信号封顶、响应不含原文、响应体小于输入 |

## 3. §17.3 硬断言 → 测试

| 硬断言 | 代表测试 |
| --- | --- |
| 工具集合是否合理 | `test_tools_adapters.test_build_tools_exposes_expected_names`、`test_search_knowledge_tool_schema_only_exposes_query` |
| 参数和错误码是否正确 | `test_input_boundaries`（上限/缺参/越界 → `INVALID_ARGUMENTS`）、`test_tools_adapters`（`RAG_TIMEOUT`/`UNKNOWN_SERVICE`） |
| observations 是否完整 | `test_graph_workflow.test_graph_executes_multiple_tool_calls_in_one_tool_node_turn`、`test_tool_result_contract.test_observe_processes_each_tool_message_exactly_once` |
| 步骤轨迹是否保留 | `test_step_persistence`（9 条）、`test_logging_and_steps`、`test_design_cases_graph.test_case_09_*` |
| 报告是否通过 Schema | `test_report_evidence`、`test_report_repair`、`test_graph_workflow.test_parse_report_maps_known_tool_names_to_business_sources` |
| 失败是否停止 | `test_graph_workflow.test_graph_stops_and_degrades_on_tool_failure`、`test_design_cases_graph.test_case_03_* / case_06_*` |
| 是否不会无限循环 | `test_graph_workflow.test_graph_stops_at_max_model_iterations`、`test_design_cases_graph.test_case_09_*` |

## 4. 其余设计章节 → 测试文件

| 测试文件 | 条数 | 设计文档章节 |
| --- | --- | --- |
| `test_api.py` | 5 | §13.1 API 一览、§13.3 API 状态码 |
| `test_ci_workflow.py` | 6 | **不属于设计文档**：守护 `.github/workflows/ci.yml` 自身（P1-4-5 的真实事故，见 §5 第 5 条） |
| `test_degraded_summary.py` | 15 | §16.5 失败降级、§11.4 超时与重试 |
| `test_degraded_summary_persistence.py` | 4 | §16.5、§12.1 MVP 记录内容 |
| `test_design_cases_graph.py` | 8 | §17.2 案例 2/3/5/6/7/9、§16.1 大输入、§16.2 工具异常 |
| `test_error_handling.py` | 34 | §13.3 API 状态码、§11.4 超时和重试、§16.2 工具异常 |
| `test_evidence_verification.py` | 23 | §10.4 证据核验 |
| `test_frontend_formatting.py` | 6 | **不属于设计文档**：守护 `web/src` 的常规排版（P1-5-1 的工程化守门，四条规则见文件 docstring） |
| `test_graph_workflow.py` | 6 | §17.2 案例 1/8/10、§9.2–§9.5 |
| `test_incident_authorization_api.py` | 4 | §13.3.1 运行记录权限、§2.3 集成原则 |
| `test_incident_service.py` | 2 | §5.1 Agent 输入、§13.2 分析请求、§6.2 `search_knowledge` |
| `test_input_boundaries.py` | 21 | §16.1 输入异常、§13.2 分析请求、§6.1–§6.3 工具参数契约 |
| `test_knowledge_base_authorizer.py` | 11 | §2.3 集成原则、§11.1 适配器接口、§13.2 |
| `test_knowledge_base_catalog_api.py` | 11 | §2.3 集成原则（Token 只转发）、§13.1（P1-6-1 新增端点，设计文档尚未收录） |
| `test_logging_and_steps.py` | 20 | §15.1、§15.2、§15.3、§15.5、§15.6 |
| `test_model_config.py` | 10 | §14 配置设计 |
| `test_model_timeout.py` | 18 | §11.4（含"已实现：模型超时、请求预算与错误归一化"） |
| `test_redaction.py` | 15 | §12.2 敏感数据规则、§12.2.1 脱敏落地位置 |
| `test_report_evidence.py` | 11 | §8.4 状态值、§10.3 证据来源规范、§10.4、§16.4 报告幻觉控制 |
| `test_report_repair.py` | 21 | §10.2 报告生成原则、§16.4 |
| `test_run_access_and_failures.py` | 14 | §13.3.1、§12.4 中断记录回收、§16.1 |
| `test_run_history_pagination.py` | 24 | §13.3.2 历史分页、过滤与保留策略 |
| `test_run_history_summary.py` | 11 | §12.1、§13.3.1、§15.2 不记录的内容 |
| `test_run_timestamps_fields.py` | 12 | §12.1、§15.5 步骤轨迹的摘要与耗时 |
| `test_stale_run_recovery.py` | 9 | §12.4 中断记录的回收 |
| `test_step_persistence.py` | 9 | §12.1、§12.5 步骤追加的幂等与并发 |
| `test_tool_result_contract.py` | 16 | §7 统一工具结果协议、§16.2 工具异常、§17.3 硬断言 |
| `test_tools_adapters.py` | 8 | §6.1 analyze_log、§6.2 search_knowledge、§6.3 get_service_status、§7 |
| `test_untrusted_content.py` | 12 | §16.3 Prompt Injection、§16.3.1 不可信内容定界与注入中和 |
| `test_utc_timestamps.py` | 6 | §12.1（时间一律 naive UTC 存储） |

> `tests/conftest.py` 不是测试用例：它只做进程级环境隔离（清掉代理变量），与业务章节无关。

## 5. 已知边界与"测不了"的部分（如实记录，不当成已完成）

1. **§17.2 案例 7「服务状态工具超时」在本项目里不可复现**：`get_service_status` 是进程内的 mock 注册表，没有网络与 I/O，因此不存在超时路径。真正会超时的外部工具只有 `search_knowledge`（`RAG_TIMEOUT`），本次用它覆盖案例 7 的**意图**（超时要被识别成超时，而不是通用失败）。要做真正的服务状态超时，需要先做 **P2-5 真实服务状态 provider**（仍未做）。
2. **真实模型返回 + 真实检索链路仍不是自动化依据**：§17.1 明确要求"真实模型不作为唯一的自动化测试依据"，本项目全部测试用 `FakeModel` / `MockRagGateway`；但**反向也成立**——自动化测试全绿不等于真实链路通过，真实 DeepSeek 返回 + 真实 `/search` 这条链路至今没有跑过（见交接文档 5.2 第 2 条）。
3. **前端没有自动化测试**：`web/` 只有 `vue-tsc` + `vite build`；组件与界面行为没有测试手段，属 **P1-5-3**。
4. **`analyze_log` 的 `MAX_MATCHED_TEXT = 120`（单条命中文本截断）在当前正则下不可达**：四个模式最长只会匹配 11 个字符（`stack trace` / `异常堆栈` 等），因此无法构造"被截断"的用例。这是防御性代码，不是漏测——记录在此以免下次被误认为缺测试。
5. **CI 的真实运行结果已确认（2026-09-15，P1-4-5 完成）**：run `34930276951`（`head_sha=8eea1fb`）两个 job 全绿，后端含 `ruff` / `pytest` / `compileall` / `alembic heads`，前端含 `npm ci` / 类型检查与构建。确认它花的时间值得：第一次读结果就发现 CI 一直是红的，根因是"测试在无 `.env` 的检出里于收集阶段即失败"与"两条用例静默依赖开发机真实 MySQL"，另有我自己引入的一处 YAML 引号缺陷（表现为 **0 job 的失败运行**）。读取方式：`httpx.Client(trust_env=False)` 直连 `api.github.com`（走开发机代理会被共享 IP 的匿名配额限流）。
6. **`tests/test_ci_workflow.py` 不在设计文档的章节体系里**：它守的是 CI 定义文件本身（能解析、两个 job 与关键步骤仍在、backend job 提供 `INCIDENT_DATABASE_URL`）。放在这里是为了让"28 个文件 → 章节"的表不漏项，而不是把它算成设计文档要求。
7. **`tests/test_frontend_formatting.py` 也不在设计文档的章节体系里**：它守的是前端源码排版（P1-5-1）。与第 6 条同理，列进上表只是为了让"测试文件 → 章节"不漏项，不算设计文档要求；它的六条规则与"压缩/展开"合成样例（含 `@import url(...)` 里分号的误报修正）都写在该文件 docstring 里。
8. **测试规模口径以命令为准**：上表的条数是 2026-09-15 的实测值，新增测试后请重新跑 `py -m pytest tests --collect-only -q` 更新。
