# 阶段 6 验收清单

## 自动化检查

- [x] 后端 `pytest` 全量通过；
- [x] `compileall` 通过；
- [x] `alembic check` 无新增迁移；
- [x] `ruff check app` 通过；
- [x] 前端 `npm run build` 通过；
- [x] 一键启动脚本能启动三个端口；
- [x] 端口已占用时跳过重复服务；
- [x] 未授权 API 返回 `401`；
- [x] Graph 正常、工具失败、报告失败、多工具和循环上限均有测试。

## 手工验收

| 场景 | 操作 | 预期 |
| --- | --- | --- |
| 登录 | Web 输入 DevAtlas 账号 | 成功进入工作台，Token 不写入 Agent 数据库 |
| 正常分析 | 知识库 `3`，订单服务 502 + MySQL 超时 | `completed`，有报告、引用、observations 和 steps |
| 未知服务 | 输入要求查询 `gateway` | `degraded`，错误码 `UNKNOWN_SERVICE` |
| RAG 无结果 | 使用无关问题或空结果 Mock | 成功返回但报告置信度降低，不能伪造引用 |
| 历史记录 | 点击运行历史并选择一条 | 能按当前用户查看对应运行记录 |
| 权限隔离 | 用另一用户访问他人 `run_id` | 返回 `404`，不泄露记录是否存在 |
| 服务停止 | 停止 DevAtlas 后提交分析 | 返回可读降级错误，不泄露 traceback |

## 交付前检查

```powershell
cd E:\IncidentAgent
git status
```

确认以下内容没有被提交：

```text
.env
JWT
DeepSeek API Key
MySQL 密码
完整生产敏感日志
```

## 已知限制

- 当前服务状态工具是显式模拟数据；
- Web 暂时是同步 JSON，不是 SSE；
- 未实现真实监控、Kubernetes、Redis、Prometheus 和自动修复；
- `datetime.utcnow()` 在 Python 3.13 会产生弃用警告，后续可单独迁移为 UTC 感知时间；
- 真实模型测试具有 API 消耗和输出波动，自动化测试使用 FakeModel。
