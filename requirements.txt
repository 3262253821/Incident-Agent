# Runtime dependencies. Loose lower bounds on purpose: this is the file GitHub's
# dependency graph and security alerts read, so it must express real ranges.
#
# For a reproducible install use requirements-lock.txt (verified on Python 3.13).
# The bounds below were validated by installing this file into a clean virtualenv
# and running the full suite: 172 passed.

openai>=3.0
python-dotenv>=1.0
pydantic>=2.0
langchain-core>=1.0
langchain-openai>=1.0
langgraph>=1.0
sqlalchemy>=2.0
pymysql>=1.1
alembic>=1.13
httpx>=0.28
fastapi>=0.115
uvicorn[standard]>=0.30
